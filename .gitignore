# Build inputs contain passwords (build/users.txt), the plaintext dataset
# (build/raw/), the unencrypted app (build/template.html), the AEM scrape
# (build/report/index.js), and the Excel snapshot. They must never be pushed
# to the PUBLIC repo — it only ever carries ciphertext.
# The private mirror tracks build/ on purpose; see build/publish.py.
build/

# Legacy / local plaintext — never commit.
.creds
live-content-data.plain.json
*.xlsx

# The deploy-root JSON file is ciphertext and IS meant to be committed:
#   index.html  live-content-data.json
# Do not add a *.json rule here.

# OS / editor noise
.DS_Store
Thumbs.db
__pycache__/
*.pyc
